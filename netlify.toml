[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/build"
  base = "/"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
