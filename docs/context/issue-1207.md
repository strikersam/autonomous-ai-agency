# Issue #1207: Refactor scheduler cleanup removal paths

_Generated: 2026-08-03_

## Context Plan — Issue #1207: Refactor scheduler cleanup removal paths

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://github.com/strikersam/autonomous-ai-agency/pull/1206 |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `z-ai/glm-5.2` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The linked artifact is GitHub issue #1207 in this repository, requesting an internal refactor of AgentScheduler.force_cleanup() to consolidate four duplicated run-once and stale-job removal branches left after PR #1206. No external URL content was provided or retrievable, so this summary is based solely on the issue body text, which must be reviewed against the actual source before implementation.

---

## Decision

✅ **ADOPT** — build largely as described in the source.

The issue is an internal maintenance refactor requested by the project owner targeting a real file in this codebase. It reduces duplication in AgentScheduler.force_cleanup() without changing scheduler behavior, aligning with CLAUDE.md §0 and the architectural principle of one source of truth per concern.

---

## Implementation Prompt

Refactor `AgentScheduler.force_cleanup()` in `packages/scheduler/scheduler.py` to consolidate the four run-once and stale-job removal branches behind a shared internal helper. The four paths are: (1) every-minute unfired run-once expiration (the `_expire_run_once_orphan()` path added in PR #1206), (2) general TTL unfired run-once expiration, (3) already-fired run-once removal, and (4) stuck agency-task removal.

Extract one or more private helper methods that perform, in order: durable-store deletion, APScheduler deregistration (only when an in-memory APScheduler job was registered for that job_id), and in-memory removal. The helper must return a boolean or raise on durable failure so callers do not increment their summary counter when the durable delete fails. Preserve each path's current eligibility criteria exactly — do not merge the WHERE clauses or timing predicates. Preserve the existing summary counter semantics: a successful removal increments the correct counter (`deleted`, `deduped`, or `expired`); a failed durable delete increments none of them.

The critical step is the durable-delete-failure handling (CLAUDE.md §14.3): re-derive the current behavior of each branch before changing it. Confirm what happens today when the durable store delete returns False or raises, and ensure the refactored helper reproduces that outcome rather than silently swallowing the error and bumping the counter. Do not report a removal as successful when durable deletion fails.

APScheduler deregistration must be conditional: only call the APScheduler remove method when an in-memory job was registered for that id. Guard against the not-found exception so a missing APScheduler job does not abort the cleanup. The tests must pass with APScheduler installed and with it absent — keep the import and call site behind the existing availability guard.

Do not change any public method signature, any API response, any log message level, or any counter name. The refactor is purely internal. After the change, `force_cleanup()` should be visibly shorter and the four branches should call the shared helper where their behavior permits; where a branch has unique eligibility logic, keep that logic inline and delegate only the removal sequence.

Assuming `_expire_run_once_orphan()` already exists in `packages/scheduler/scheduler.py` as a method on `AgentScheduler` (unverified) — if it is a free function or lives on a different class, adjust the helper to call it via the correct receiver. Assuming the durable store exposes a `delete_job(job_id)` method returning a truthy or falsy value (unverified) — if it raises instead of returning False, the helper's failure detection must use try/except rather than a falsy check. Assuming the scheduler tests live at `packages/scheduler/test_scheduler.py` (unverified) — if they live at a different path, update step 7's file field accordingly.

---

## TODO List

- [ ] **[M]** Read `packages/scheduler/scheduler.py` and enumerate the four removal branches inside `force_cleanup()`, capturing each branch's eligibility predicate, the counter it increments, and the current durable-delete + APScheduler-deregistration + in-memory-removal sequence. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ A written table exists in the PR description listing, for each of the four branches: the eligibility condition, the counter name, and the exact removal calls made today.
- [ ] **[M]** Extract a private `_remove_job` helper on `AgentScheduler` that performs durable deletion, APScheduler deregistration (guarded by an in-memory-job-registered check and a not-found exception handler), and in-memory removal, returning success or failure so a failed durable delete is not counted as a removal. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The helper method exists, has a typed signature, and its docstring states that a False return or raised exception means the durable delete failed and the caller must not increment its counter.
- [ ] **[M]** Rewire the every-minute unfired run-once expiration branch (the `_expire_run_once_orphan()` path) to call `_remove_job`, preserving its eligibility predicate and `expired` counter semantics. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The branch calls `_remove_job` for the removal sequence, increments `expired` only on helper success, and the eligibility predicate is unchanged from step 1's table.
- [ ] **[M]** Rewire the general TTL unfired run-once expiration branch to call `_remove_job`, preserving its eligibility predicate and `expired` counter semantics. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The branch calls `_remove_job`, increments `expired` only on helper success, and the eligibility predicate is unchanged from step 1's table.
- [ ] **[M]** Rewire the already-fired run-once removal branch to call `_remove_job`, preserving its eligibility predicate and `deleted` or `deduped` counter semantics. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The branch calls `_remove_job`, increments the correct counter only on helper success, and the eligibility predicate is unchanged from step 1's table.
- [ ] **[M]** Rewire the stuck agency-task removal branch to call `_remove_job`, preserving its eligibility predicate and `deleted` counter semantics. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The branch calls `_remove_job`, increments `deleted` only on helper success, and the eligibility predicate is unchanged from step 1's table.
- [ ] **[L]** Add or extend regression tests covering: a failed durable delete does not increment `deleted`, `deduped`, or `expired` for any of the four paths; APScheduler deregistration is attempted only when an in-memory job was registered; and the every-minute and daily run-once cleanup behavior is unchanged. — `packages/scheduler/scheduler.py`
      <br/>_Done when:_ The focused scheduler tests pass with APScheduler installed and with APScheduler absent, and the new tests assert the counter values are zero on durable-delete failure.

---

## Relevant Files to Read First

- `packages/scheduler/scheduler.py`

---

## Risk Flags

- ⚠️ `packages/scheduler/scheduler.py`

---

## Architectural Notes

No external source was retrieved; the issue body is the only artifact and must be reviewed against the actual source before implementation. Assuming `_expire_run_once_orphan()` is a method on `AgentScheduler` in `packages/scheduler/scheduler.py` (unverified) — if it is a free function or on a different class, the helper call site in step 3 must use the correct receiver. Assuming the durable store exposes `delete_job(job_id)` returning a truthy or falsy value (unverified) — if it raises on failure instead, the helper's failure detection must use try/except. The exact counter names (`deleted`, `deduped`, `expired`) are taken from the issue body; confirm against the current `force_cleanup()` return dict before finalizing. The scheduler test file path is unverified — the implementing agent must locate the actual test file and update step 7's file field accordingly.

---

## Quality Gate

⚠️ **1 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation

---

*Closes #1207*

