# Issue #517: Extend QuickNotes FAB into a Capture hub: notes, bugs, ideas with association

_Generated: 2026-06-11_

## Context Plan — Issue #517: Extend QuickNotes FAB into a Capture hub: notes, bugs, ideas with association

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement the Capture Hub feature as described in GitHub Issue #517. **Key Tasks:**
1. **Modify FAB UI (remote-admin/):** Update the FAB in `remote-admin/src/components/FloatingActionButton.js` to include a type selector (Note, Bug, Idea, Task) with Note as default. Ensure one-tap selection for Note.
2. **Auto-Captured Context:** Enhance `agent/tools.py` (RISKY) to auto-capture context (screen, route, timestamp, and `run_id` for Tasks) for every entry. **Assumption:** Utilize existing `state.py` for session context.
3. **Routing Logic for Bug Type:** In `router/model_router.py`, add logic to route Bug types to GitHub issues with `bug` and `repo-linked` (if repo behavior is referenced) labels. Pre-fill issue body with captured context and optional screenshot field.
4. **Idea/Task Filing:** Update `chat_handlers.py` to file Idea/Task as quick-note issues but with `idea`/`task` labels.
5. **Knowledge Screen - Captures Tab:** In `admin_auth.py` (RISKY) and `remote-admin/`, create a Captures tab listing entries with associated screen and linked issue/run. **Assumption:** Leverage existing dashboard infrastructure.
6. **Testing:** Ensure acceptance criteria are met, especially for the Doctor screen scenario. **Coding Rules Compliance:** Ensure type annotations, async I/O, logging, and Pydantic models are used. Update `docs/changelog.md` under [Unreleased].

---

## TODO List

- [ ] **[M]** Update FAB UI with type selector `remote-admin/src/components/FloatingActionButton.js`
- [ ] **[L]** Enhance auto-capture context in agent/tools.py `agent/tools.py`
- [ ] **[L]** Implement Bug routing logic in model_router.py `router/model_router.py`
- [ ] **[S]** Update chat_handlers.py for Idea/Task filing `chat_handlers.py`
- [ ] **[M]** Create Captures tab in Knowledge screen `admin_auth.py`
- [ ] **[M]** Write unit/tests for new functionality ``
- [ ] **[S]** Update docs/changelog.md `docs/changelog.md`
- [ ] **[S]** Run pytest -x before and after changes ``

---

## Relevant Files to Read First

- `remote-admin/src/components/FloatingActionButton.js`
- `agent/tools.py`
- `router/model_router.py`
- `chat_handlers.py`
- `admin_auth.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/tools.py
- ⚠️ admin_auth.py

---

## Architectural Notes

Assuming `state.py` adequately stores session context for auto-capture. Verify GitHub API handling for Bug issue creation. Ensure async I/O in all new API interactions.

---

*Closes #517*

