# Issue #621: Investigate CI Syntax check null-byte false positive on scripts/build_tech_db.py

_Generated: 2026-06-14_

## Context Plan — Issue #621: Investigate CI Syntax check null-byte false positive on scripts/build_tech_db.py

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a solution to address the deterministic SyntaxError false positive in `scripts/build_tech_db.py` during CI runs, focusing on suspected causes: (1) runner-side line-ending transforms, (2) force-push artifacts, or (3) checkout hooks. Given the file's cleanliness and the failure's reproducibility on rebased PRs, your task involves: 
1. **Investigating Line Endings**: Modify `.github/workflows/ci.yml` to explicitly handle line endings before the syntax check step. Utilize `actions/checkout` with `lfs: true` and consider adding a step to normalize line endings (e.g., using `dos2unix`) specifically for `scripts/build_tech_db.py` before the syntax check.
2. **Force-Push Artifact Mitigation**: Since direct Git manipulation in CI is tricky, document in `docs/troubleshooting.md` a pre-CI checkout verification step for developers to run locally (e.g., a simple `git ls-files -v scripts/build_tech_db.py` to detect smudge issues) and suggest using `git update-index --assume-unchanged scripts/build_tech_db.py` temporarily for troubled files, then `--no-assume-unchanged` before pushing.
3. **Checkout Hook Analysis**: While CI doesn’t directly run local hooks, ensure `.claude/hooks/pre-commit` doesn’t inadvertently affect the file (though unlikely in this CI context). 
**Implementation Steps**:
- In `.github/workflows/ci.yml`, before the syntax check, add a step to run `dos2unix scripts/build_tech_db.py` (ensure `dos2unix` is installed in the CI environment).
- Update `docs/troubleshooting.md` with pre-CI checks for developers.
- Verify no unintended effects from `.claude/hooks/pre-commit` on `scripts/build_tech_db.py`.
**Coding Rules Compliance**:
- Ensure all new functions have type annotations.
- Log actions using `logging`.
- No secrets in new code; config via env vars if needed.
**Files to Modify**:
- `.github/workflows/ci.yml`
- `docs/troubleshooting.md`
- Optionally, `.claude/hooks/pre-commit` if necessary (unlikely).


---

## TODO List

- [ ] **[M]** Modify .github/workflows/ci.yml to handle line endings and add dos2unix step `.github/workflows/ci.yml`
- [ ] **[S]** Update docs/troubleshooting.md with developer pre-CI checks `docs/troubleshooting.md`
- [ ] **[S]** Review .claude/hooks/pre-commit for unintended file effects `.claude/hooks/pre-commit`
- [ ] **[S]** Run pytest -x before and after changes ``
- [ ] **[S]** Update docs/changelog.md under [Unreleased] `docs/changelog.md`

---

## Relevant Files to Read First

- `.github/workflows/ci.yml`
- `docs/troubleshooting.md`
- `.claude/hooks/pre-commit`
- `scripts/build_tech_db.py`

---

## Risk Flags

_No risky modules flagged._

---

## Architectural Notes

Assuming CI environment supports `dos2unix`. If not, an alternative line ending normalization method must be used. Investigate if `dos2unix` needs explicit installation in the CI setup.

---

*Closes #621*

