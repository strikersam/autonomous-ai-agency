# Issue #656: Bugs

_Generated: 2026-06-17_

## Context Plan — Issue #656: Bugs

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement the following fixes for the local-llm-server project to address agent task blocking and HTTP error handling issues (GitHub Issue #656). Ensure adherence to the project's coding rules.

1. **Modify `agent/loop.py`**: Enhance the `AgentRunner` class to handle task blocking gracefully. Introduce a `max_retry` parameter (default=5) and implement exponential backoff (initial wait 5s, max wait 5min) for retries. Update the `execute_task` method to catch and log specific exceptions (e.g., `HTTPError` for 400, 429, 502 errors) using `logging.error`. Ensure type annotations are updated.

2. **Update `handlers/anthropic_compat.py`**: In the `AnthropicCompatHandler`, wrap the API call to `https://api.anthropic.com/v1/messages` in a retry mechanism (3 attempts with 2s delay between attempts) using `tenacity` decorator (`@retry` with `stop_max_attempt_number` and `wait_exponential`). Handle `requests.exceptions.HTTPError` and log errors with context.

3. **Enhance Error Logging in `agent/tools.py`**: Modify the `WorkspaceTools` class to log detailed error messages (including response bodies if available) for HTTP errors, facilitating easier debugging.

4. **Docs & Changelog**: Update `docs/changelog.md` under [Unreleased] with a brief description of the fixes. Ensure all changes are properly documented in relevant module `CLAUDE.md` files if necessary.

---

## TODO List

- [ ] **[M]** Implement task retry with backoff in `agent/loop.py` `agent/loop.py`
- [ ] **[S]** Add retry to Anthropic API calls in `handlers/anthropic_compat.py` `handlers/anthropic_compat.py`
- [ ] **[S]** Enhance HTTP error logging in `agent/tools.py` `agent/tools.py`
- [ ] **[S]** Update docs/changelog.md and relevant CLAUDE.md files `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` and then `pytest -v` to ensure no regressions ``

---

## Relevant Files to Read First

- `agent/loop.py`
- `handlers/anthropic_compat.py`
- `agent/tools.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ handlers/anthropic_compat.py

---

## Architectural Notes

Assumption: `tenacity` is available. If not, add it to `requirements.txt`. Ensure logging configurations capture the new error details effectively.

---

*Closes #656*

