# Issue #513: quick-note:https://github.com/chopratejas/headroom

_Generated: 2026-06-10_

## Context Plan — Issue #513: quick-note:https://github.com/chopratejas/headroom

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

You are implementing a new feature to integrate Headroom's context compression capabilities into the local-llm-server. The goal is to add a new proxy mode that can compress prompts before sending them to Ollama, reducing token usage while maintaining answer quality. You must create a new module for compression logic that can be toggled via a new API parameter. Start by creating a new file `compression/compressor.py` that implements the actual compression logic using the Headroom approach. This should include content type detection and selection of appropriate compression algorithms (SmartCrusher for JSON, CodeCompressor for code, Kompressor for text). The compressed output should maintain the same interface as the original prompt but with fewer tokens. You also need to modify `proxy.py` to add a new route `/v1/compressed-chat/completions` that handles the compressed streaming. Update the OpenAPI schema in `chat_handlers.py` to support a new `compress` boolean parameter in the request. When `compress=true`, the system should route the prompt through the compression layer before forwarding to Ollama. The compression module should be pluggable and not affect existing non-compressed flows. All new code must follow project standards: use type hints, async I/O, and Pydantic models. The compression logic should be isolated in its own module and not touch auth, key_store, or agent modules. Update docs/changelog.md to include the new compression feature. Run `pytest -x` to ensure all tests pass after your changes.

---

## TODO List

- [ ] **[L]** Create compression/compressor.py with content type detection and compression algorithms `compression/compressor.py`
- [ ] **[M]** Add new route in proxy.py for compressed chat completions `proxy.py`
- [ ] **[M]** Update OpenAPI schema in chat_handlers.py to support compress parameter `chat_handlers.py`
- [ ] **[S]** Implement Pydantic models for compression I/O in new compression/models.py `compression/models.py`
- [ ] **[M]** Write unit tests for the new compression module `tests/test_compression.py`
- [ ] **[S]** Update docs/changelog.md with the new compression feature `docs/changelog.md`

---

## Relevant Files to Read First

- `proxy.py`
- `chat_handlers.py`
- `compression/compressor.py`
- `compression/models.py`

---

## Risk Flags

_No risky modules flagged._

---

## Architectural Notes

The compression feature should be built as a self-contained module that does not interfere with existing auth, routing, or agent logic. It must be possible to toggle compression on/off via the API. The implementation should follow Headroom's architecture of CacheAligner → ContentRouter → CCR, but simplified for this codebase.

---

*Closes #513*

