        # Issue #402: CI: Failing Tests — 2026-06-05

        _Generated: 2026-06-05_

                ## Context Plan — Issue #402: CI: Failing Tests — 2026-06-05

        > Auto-generated by the **issue-context-generator** workflow.
        > This is a **DRAFT PR** — implement by triggering the Process Quick Note
        > workflow or by opening a Claude Code session on this branch.

        ---

        ## Implementation Prompt

        You are implementing fixes for a critical test failure in the local-llm-server project. The test suite is currently failing due to 40+ test cases, primarily in `test_activity_logs.py` and `test_chat_mode_regressions.py`. Your task is to identify and resolve the root causes of these failures.

First, examine the failing tests listed in the issue. Focus on:
1. `test_activity_endpoint_includes_recent_error_logs` in `tests/test_activity_logs.py` — this test ensures error logs are correctly exposed via the activity API. Diagnose why the activity endpoint is not returning the expected error log entries.
2. Multiple agent-related tests in `tests/test_chat_mode_regressions.py` — these test chat mode behaviors, agent handoff logic, and server-sent event streaming. Investigate whether recent changes to agent state management, task routing, or session persistence have broken expected behavior.

Use the codebase map in CLAUDE.md to understand the structure:
- `proxy.py` is the main app entrypoint
- `agent/loop.py` and `agent/state.py` manage agent execution and session state
- `router/model_router.py` handles model selection
- `key_store.py` and `admin_auth.py` are flagged as high-risk auth modules

Ensure all fixes follow project rules:
- Add type hints to all new/modified functions
- Use Pydantic models for API I/O
- Maintain async I/O
- Log errors via `logging` module, not `print`
- Do not commit secrets to source

Verify fixes by running `pytest -x` until all originally failing tests pass. Update `docs/changelog.md` with a concise description of the behavioral fix under [Unreleased].

        ---

        ## TODO List

        - [ ] **[M]** Debug and fix activity endpoint test: ensure recent error logs are captured and returned `tests/test_activity_logs.py`
- [ ] **[L]** Fix agent handoff logic: verify agent mode routing and session persistence `agent/loop.py`
- [ ] **[L]** Verify chat mode regression tests: direct path prompt handling, SSE emission, and agent status reporting `tests/test_chat_mode_regressions.py`
- [ ] **[M]** Ensure model router correctly handles task classification and routing in non-agent mode `router/model_router.py`
- [ ] **[S]** Update changelog with a summary of test fixes under [Unreleased] section `docs/changelog.md`
- [ ] **[S]** Run full test suite with `pytest -x` to confirm no regressions ``

        ---

        ## Relevant Files to Read First

        - `tests/test_activity_logs.py`
- `tests/test_chat_mode_regressions.py`
- `agent/loop.py`
- `router/model_router.py`
- `agent/state.py`

        ---

        ## Risk Flags

        - ⚠️ key_store.py
- ⚠️ admin_auth.py
- ⚠️ agent/loop.py

        ---

        ## Architectural Notes

        The failing tests suggest a disconnect between agent state persistence, activity log retrieval, and chat mode routing. Focus on integration points between agent/ and router/ modules.

        ---

        *Closes #402*

