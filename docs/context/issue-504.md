# Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

_Generated: 2026-06-13_

## Context Plan — Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a persistent scheduler store using a file-based JSON solution to prevent loss of schedules on redeployment. Ensure the store is loaded on startup and saved on changes. Modify `service_manager.py` to interact with this new store. Implement async read/write operations with proper logging and error handling. Update `docs/changelog.md` under [Unreleased]. Ensure type annotations, no secrets, and Pydantic models if applicable. Run `pytest -x` before and after changes.

---

## TODO List

- [ ] **[M]** Create a new module `scheduler_store.py` for JSON file operations (async read/write with logging) `scheduler_store.py`
- [ ] **[M]** Modify `service_manager.py` to use `scheduler_store.py` for loading and saving schedules `service_manager.py`
- [ ] **[L]** Write unit tests for `scheduler_store.py` and integration tests for schedule persistence in `service_manager.py` `tests/test_scheduler_store.py, tests/test_service_manager.py`
- [ ] **[S]** Update `docs/changelog.md` under [Unreleased] with the new feature `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` to ensure no regressions ``

---

## Relevant Files to Read First

- `service_manager.py`
- `scheduler_store.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ service_manager.py

---

## Architectural Notes

Consider future scalability with a database for very large schedules. Ensure JSON file path is configurable via env vars for different environments.

---

*Closes #504*

