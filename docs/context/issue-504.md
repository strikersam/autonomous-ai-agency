# Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

_Generated: 2026-06-10_

## Context Plan — Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

You are implementing fixes for the autonomy hardening EPIC. Focus on these key areas: 1) Make scheduler store persistent instead of ephemeral by implementing a file-based or database-backed scheduler, 2) Ensure orchestrator runs have access to GitHub tokens by modifying the agent execution context, 3) Add worktree isolation for concurrent runs, 4) Add edit UI for providers screen, 5) Make orchestrator runs visible in dashboard, 6) Fix doctor endpoint to properly separate public vs authenticated company counts, 7) Make onboarding idempotent to prevent duplicate companies, 8) Fix scanner silent stall issue. For the scheduler persistence, modify the apscheduler job store in scripts/ai_runner.py to use a persistent job store like SQLAlchemyJobStore. For GitHub token access, ensure the agent context in agent/loop.py passes the token through the execution environment. For worktree isolation, modify agent/tools.py to create isolated worktrees per task. For the providers edit UI, add CRUD endpoints in proxy.py and update the admin dashboard in remote-admin/. For orchestrator run visibility, update the dashboard in remote-admin/ to show active runs. For onboarding idempotency, ensure the onboarding logic checks for existing companies before creation in the company management module. For the scanner fix, investigate the stall at scripts/ai_runner.py and add better error handling. All changes must be async, use proper logging, and follow the project's coding rules. Type annotations are required.

---

## TODO List

- [ ] **[M]** Implement persistent scheduler store in ai_runner.py to prevent schedule wipe on redeploy `scripts/ai_runner.py`
- [ ] **[M]** Add GitHub token to agent execution context in loop.py `agent/loop.py`
- [ ] **[L]** Implement worktree isolation in tools.py `agent/tools.py`
- [ ] **[M]** Add provider edit UI to admin dashboard in remote-admin/ `remote-admin/components/ProvidersEdit.vue`
- [ ] **[M]** Add UI for orchestrator run visibility in dashboard `remote-admin/components/OrchestratorRuns.vue`
- [ ] **[S]** Fix company count split in doctor endpoint in handlers/doctor.py `handlers/doctor.py`
- [ ] **[M]** Make onboarding idempotent in company_onboarding.py `company_onboarding.py`
- [ ] **[L]** Fix scanner silent stall at 88% in scanner.py `scanner.py`
- [ ] **[M]** Fix skill library loading in skill_library.py `skill_library.py`

---

## Relevant Files to Read First

- `scripts/ai_runner.py`
- `agent/loop.py`
- `agent/tools.py`
- `proxy.py`
- `remote-admin/`
- `handlers/doctor.py`
- `company_onboarding.py`
- `scanner.py`
- `skill_library.py`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ agent/tools.py
- ⚠️ key_store.py
- ⚠️ admin_auth.py

---

## Architectural Notes

The changes span multiple modules and require careful coordination. Security modules like key_store and admin_auth are at risk due to token handling. Agent modules are at risk due to execution context changes. Verify all async patterns and error handling.

---

*Closes #504*

