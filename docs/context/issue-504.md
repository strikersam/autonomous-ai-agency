# Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

_Generated: 2026-06-13_

## Context Plan — Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement fixes for verified defects in the local-llm-server project. The defects include ephemeral scheduler store, missing GitHub token for user-initiated orchestrator runs, lack of per-task worktree isolation, and others. Start by addressing the scheduler store issue in `agent/loop.py`. Modify the `AgentRunner` to use a persistent store for schedules. Ensure that the store is properly initialized and updated in `agent/state.py`. Next, focus on adding GitHub token support for user-initiated orchestrator runs in `agent/tools.py`. Update the `WorkspaceTools` to handle GitHub token authentication. For per-task worktree isolation, modify `repowise.py` to create separate worktrees for each task. Update `router/model_router.py` to route tasks to their respective worktrees. Implement edit UI for providers screen in `admin_auth.py` and update `langfuse_obs.py` to handle orchestrator run visibility. Fix the doctor public vs authenticated company-count split issue in `health.py`. Make onboarding idempotent and prevent duplicate companies in `agent/loop.py`. Resolve the scanner silent stall issue in `agent/tools.py`. Load skills from the Skill Library in production in `router/classifier.py`. Finally, address chronic CI failures by updating `scripts/ai_runner.py` and `docs/changelog.md` to reflect the changes. Use type annotations, Pydantic models, and async I/O throughout the implementation. Log changes with `logging` and update `docs/changelog.md` under the [Unreleased] section. Run `pytest -x` before and after making changes.

---

## TODO List

- [ ] **[M]** Implement persistent scheduler store `agent/loop.py`
- [ ] **[M]** Add GitHub token support for user-initiated orchestrator runs `agent/tools.py`
- [ ] **[L]** Create separate worktrees for each task `repowise.py`
- [ ] **[S]** Implement edit UI for providers screen `admin_auth.py`
- [ ] **[S]** Update Langfuse to handle orchestrator run visibility `langfuse_obs.py`
- [ ] **[M]** Fix doctor public vs authenticated company-count split `health.py`
- [ ] **[M]** Make onboarding idempotent and prevent duplicate companies `agent/loop.py`
- [ ] **[M]** Resolve scanner silent stall issue `agent/tools.py`
- [ ] **[S]** Load skills from Skill Library in production `router/classifier.py`
- [ ] **[L]** Address chronic CI failures `scripts/ai_runner.py`
- [ ] **[S]** Update docs/changelog.md `docs/changelog.md`
- [ ] **[S]** Run pytest -x before and after changes ``

---

## Relevant Files to Read First

- `agent/loop.py`
- `agent/tools.py`
- `repowise.py`
- `admin_auth.py`
- `langfuse_obs.py`
- `health.py`
- `router/classifier.py`
- `scripts/ai_runner.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ agent/tools.py
- ⚠️ admin_auth.py

---

## Architectural Notes

Assuming that the GitHub issue provides a comprehensive list of defects to be addressed. The implementation plan focuses on the most critical issues first. The `agent/loop.py` and `agent/tools.py` files are modified to address the scheduler store and GitHub token issues. The `repowise.py` file is updated to create separate worktrees for each task. The `admin_auth.py` file is modified to implement the edit UI for providers screen. The `langfuse_obs.py` file is updated to handle orchestrator run visibility. The `health.py` file is modified to fix the doctor public vs authenticated company-count split issue. The `router/classifier.py` file is updated to load skills from the Skill Library in production. The `scripts/ai_runner.py` file is modified to address chronic CI failures. The `docs/changelog.md` file is updated to reflect the changes.

---

*Closes #504*

