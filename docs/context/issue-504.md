# Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

_Generated: 2026-06-13_

## Context Plan — Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a persistent storage solution for the scheduler to retain schedules across redeployments. Ensure the chosen storage (e.g., SQLite, PostgreSQL via SQLAlchemy) integrates with the existing FastAPI framework. Modify `service_manager.py` to interact with this new store. Create a `SchedulerStore` model in `models.py` for type safety. Update `agent/loop.py` to fetch and update schedules from this persistent store. Implement CRUD operations for schedules in `service_manager.py` with appropriate logging and error handling. Ensure all public functions have type annotations and utilize async I/O where applicable.

---

## TODO List

- [ ] **[M]** Choose and set up a database (e.g., SQLite) for scheduler storage `requirements.txt`
- [ ] **[S]** Create `SchedulerStore` Pydantic model in `models.py` `models.py`
- [ ] **[L]** Modify `service_manager.py` for CRUD operations with the new store `service_manager.py`
- [ ] **[M]** Update `agent/loop.py` to use the persistent scheduler store `agent/loop.py`
- [ ] **[L]** Write tests for scheduler persistence across redeploy `tests/test_service_manager.py`
- [ ] **[S]** Update `docs/changelog.md` under [Unreleased] `docs/changelog.md`

---

## Relevant Files to Read First

- `service_manager.py`
- `models.py`
- `agent/loop.py`
- `tests/test_service_manager.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ service_manager.py

---

## Architectural Notes

Assuming SQLite for simplicity. If using a different DB, adjust `requirements.txt` and the database setup accordingly. Ensure database credentials are handled via environment variables.

---

*Closes #504*

