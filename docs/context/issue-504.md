# Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

_Generated: 2026-06-13_

## Context Plan — Issue #504: EPIC: Autonomy hardening — live-verified defects 2026-06-10

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement the following fixes for the local-llm-server project, adhering to the coding rules provided in CLAUDE.md:

1. **Persistent Scheduler Store**: Modify `agent/loop.py` and `state.py` to use a persistent store (e.g., SQLite via `sqlalchemy`) for schedules, ensuring data survives redeployments. Create a new model in `models.py` for Schedule entities.

2. **GitHub Token for User-Initiated Orchestrator Runs**: Update `agent/loop.py` to securely pass and store GitHub tokens (via `key_store.py` for storage, ensuring no secrets in source) for runs initiated by users, enabling PR creation. Add a `github_token` field to the relevant Pydantic model in `models.py`.

3. **Per-Task Worktree Isolation**: Enhance `tools.py` (specifically, `WorkspaceTools`) to create isolated worktrees for concurrent runs, preventing shared workspace issues. Utilize `pathlib` for directory management and ensure async safety.

**Testing**: After implementation, run `pytest -x` to ensure no regressions, especially focusing on new test cases for these features.

**Documentation**: Update `docs/changelog.md` under [Unreleased] with brief descriptions of these fixes.

---

## TODO List

- [ ] **[M]** Implement Persistent Scheduler Store `agent/loop.py`
- [ ] **[S]** Add GitHub Token Handling `models.py`
- [ ] **[L]** Ensure Per-Task Worktree Isolation `tools.py`
- [ ] **[M]** Write New/Update Existing Tests ``
- [ ] **[S]** Update Changelog `docs/changelog.md`

---

## Relevant Files to Read First

- `agent/loop.py`
- `state.py`
- `models.py`
- `key_store.py`
- `tools.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ key_store.py
- ⚠️ agent/loop.py

---

## Architectural Notes

Assumption: SQLite is acceptable for the scheduler store. If another DB is preferred, adjustments will be needed. Ensure key_store.py securely handles the new GitHub token storage without exposing secrets.

---

*Closes #504*

