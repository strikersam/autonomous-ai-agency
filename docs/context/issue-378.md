# Issue #378: quick-note:https://www.marktechpost.com/2026/06/02/tinyfish-launches-bigset-an-open-source-multi-agent-system-that-builds-structured-live-datasets-from-plain-english-descriptions/

_Generated: 2026-06-20_

## Context Plan — Issue #378: quick-note:https://www.marktechpost.com/2026/06/02/tinyfish-launches-bigset-an-open-source-multi-agent-system-that-builds-structured-live-datasets-from-plain-english-descriptions/

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a new endpoint in `proxy.py` that accepts a natural-language description as input and returns a structured, exportable dataset built from live web data using the BigSet multi-agent system. The system should infer the schema, dispatch agents to gather data, deduplicate results, and produce a downloadable CSV or XLSX file. Integrate the BigSet system with the existing `agent/` module, using the `loop.py` file as a reference for the agent execution cycle. Update the `models.py` file to include Pydantic models for the BigSet API I/O. Ensure that all I/O operations are asynchronous and use the `logging` module for logging. Update the `docs/changelog.md` file to reflect the new feature under the [Unreleased] section. Run `pytest -x` before and after making changes to ensure that the tests pass.

---

## TODO List

- [ ] **[M]** Create a new endpoint in `proxy.py` for BigSet integration `proxy.py`
- [ ] **[M]** Implement schema inference using Claude Sonnet via OpenRouter `agent/loop.py`
- [ ] **[L]** Integrate BigSet with the existing `agent/` module `agent/loop.py`
- [ ] **[S]** Update `models.py` to include Pydantic models for BigSet API I/O `models.py`
- [ ] **[S]** Ensure asynchronous I/O operations and logging with `logging` module `proxy.py`
- [ ] **[S]** Update `docs/changelog.md` to reflect the new feature `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` to ensure tests pass ``

---

## Relevant Files to Read First

- `proxy.py`
- `agent/loop.py`
- `models.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ key_store.py

---

## Architectural Notes

The BigSet integration requires careful consideration of the existing `agent/` module and the `key_store.py` file, which handles API key persistence. Ensure that the new endpoint is properly authenticated and authorized using the existing `admin_auth.py` module.

---

*Closes #378*

