        # Issue #362: Nvidia repo setup

        _Generated: 2026-06-05_

                ## Context Plan — Issue #362: Nvidia repo setup

        > Auto-generated by the **issue-context-generator** workflow.
        > This is a **DRAFT PR** — implement by triggering the Process Quick Note
        > workflow or by opening a Claude Code session on this branch.

        ---

        ## Implementation Prompt

        You are implementing enhancements to the local-llm-server based on features from the NVIDIA Nemotron repository. Your task is to analyze the Nemotron repository and integrate any unique and implementable features into this codebase. Focus on areas like model routing, agent orchestration, and observability. You must maintain full compatibility with the existing FastAPI-based proxy structure. All new functionality should be implemented in an async manner, with proper type annotations using `from __future__ import annotations`. Use Pydantic models for all new API I/O. Any new routing logic should be added to `router/model_router.py` and any new agent logic to `agent/loop.py`. Ensure all new features are properly logged using the `logging` module, not `print`. Do not introduce any hardcoded secrets; all configuration must come from environment variables. Add new environment variables to `scripts/ai_runner.py` if needed. Update `docs/changelog.md` under [Unreleased] with a summary of new features. Run `pytest -x` to ensure no regressions are introduced.

        ---

        ## TODO List

        - [ ] **[L]** Analyze NVIDIA Nemotron repository for unique features ``
- [ ] **[M]** Identify and document implementable features from Nemotron `docs/CLAUDE.md`
- [ ] **[M]** Implement new model routing logic in model_router.py `router/model_router.py`
- [ ] **[L]** Add new agent orchestration logic in agent/loop.py `agent/loop.py`
- [ ] **[M]** Update Langfuse observability with new tracing features if applicable `langfuse_obs.py`
- [ ] **[S]** Ensure new features are compatible with existing async FastAPI structure `proxy.py`
- [ ] **[M]** Add new Pydantic models for any new API I/O in agent/models.py `agent/models.py`
- [ ] **[S]** Update documentation and changelog for new features `docs/changelog.md`
- [ ] **[S]** Run full test suite to ensure no regressions `pytest`

        ---

        ## Relevant Files to Read First

        - `router/model_router.py`
- `agent/loop.py`
- `agent/models.py`
- `langfuse_obs.py`
- `proxy.py`

        ---

        ## Risk Flags

        - ⚠️ agent/loop.py
- ⚠️ key_store.py
- ⚠️ admin_auth.py

        ---

        ## Architectural Notes

        Focus on non-breaking enhancements that improve local LLM routing and agent orchestration without altering core proxy behavior. Ensure all new features respect the existing async architecture and do not introduce hardcoded secrets.

        ---

        *Closes #362*

