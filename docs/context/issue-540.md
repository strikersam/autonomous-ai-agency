# Issue #540: The MCP server menu is missing.

_Generated: 2026-06-12_

## Context Plan — Issue #540: The MCP server menu is missing.

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement the restoration of the MCP integration options menu within the local-llm-server's admin dashboard. **Assumption:** The MCP menu was previously functional and its removal was unintended, likely due to a recent code update affecting the admin dashboard (remote-admin/). **Steps for AI Coding Agent:** 1. **Review History:** Analyze Git history to identify the commit where the MCP menu was last present in `remote-admin/` (e.g., search for 'MCP' in deleted/modified files). 2. **Restore/Update Menu Template:** a. If the menu template (e.g., `mcp_menu.html`) was deleted, restore it from the identified commit or recreate based on the last known functional version. b. Ensure the template is updated to match current UI standards (refer to other menus in `remote-admin/` for consistency). 3. **Re-enable Menu Routing:** In `proxy.py`, check for and restore/re-add the route handling the MCP menu (likely a missing or commented-out `@app.get` or similar for the MCP endpoint). Ensure the route points to the correct handler in `admin_auth.py` or a new module if the logic was moved. 4. **Security & Auth Check:** Verify in `admin_auth.py` that the restored menu and its actions are properly gated behind admin authentication. 5. **Testing:** a. **Unit Tests:** Add tests in `tests/test_admin_routes.py` to ensure the MCP menu route responds correctly and is authenticated. b. **E2E Testing:** Manually verify the menu's presence and functionality post-deployment to the dev environment. 6. **Documentation:** Update `docs/admin_dashboard.md` to reflect the MCP menu's return, including any new configuration options or behaviors. 7. **Changelog:** Add an entry under `[Unreleased]` in `docs/changelog.md` detailing the restoration of the MCP server menu.

---

## TODO List

- [ ] **[M]** Analyze Git history for MCP menu removal ``
- [ ] **[S]** Restore/update MCP menu template in remote-admin/ `remote-admin/mcp_menu.html`
- [ ] **[S]** Re-enable MCP menu route in proxy.py `proxy.py`
- [ ] **[M]** Verify security/auth for MCP menu in admin_auth.py `admin_auth.py`
- [ ] **[S]** Write unit tests for MCP menu route `tests/test_admin_routes.py`
- [ ] **[M]** Manually test MCP menu in dev environment ``
- [ ] **[S]** Update admin dashboard docs `docs/admin_dashboard.md`
- [ ] **[S]** Update changelog `docs/changelog.md`

---

## Relevant Files to Read First

- `proxy.py`
- `admin_auth.py`
- `remote-admin/mcp_menu.html`
- `tests/test_admin_routes.py`
- `docs/admin_dashboard.md`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ admin_auth.py

---

## Architectural Notes

Assuming the MCP menu's backend logic remains intact; if not, additional steps in the agent/ or router/ modules might be necessary. Verify no unintended exposure of MCP config/options due to auth changes.

---

*Closes #540*

