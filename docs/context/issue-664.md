# Issue #664: quick-note:https://github.com/Grominet95/jarvis-OS

_Generated: 2026-06-17_

## Context Plan — Issue #664: quick-note:https://github.com/Grominet95/jarvis-OS

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a real-time voice pipeline using LiveKit, similar to the one described in the Jarvis OS repository. This should allow for voice commands to be sent to the local-llm-server and processed in real-time. The pipeline should include STT (Whisper/Deepgram) + LLM + TTS (Piper/ElevenLabs) and be bridged via LiveKit. Additionally, integrate the Memory Kernel feature from Jarvis OS, which allows for faits atomiques datés, sourcés, renforçables, oubliables, corrigeables, and utilize SQLite as the source of truth with a Markdown mirror for readability. Ensure that the implementation follows the coding rules, including type annotations, no secrets in source, Pydantic models for API I/O, async for all I/O, and logging with `logging`. Update the docs/changelog.md under [Unreleased] and run `pytest -x` before and after changes.

---

## TODO List

- [ ] **[L]** Research and implement LiveKit for real-time voice pipeline `proxy.py`
- [ ] **[L]** Integrate STT (Whisper/Deepgram) + LLM + TTS (Piper/ElevenLabs) into the pipeline `handlers/anthropic_compat.py`
- [ ] **[M]** Implement Memory Kernel feature with SQLite and Markdown mirror `models.py`
- [ ] **[S]** Update docs/changelog.md under [Unreleased] `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` before and after changes ``

---

## Relevant Files to Read First

- `proxy.py`
- `handlers/anthropic_compat.py`
- `models.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ proxy.py
- ⚠️ handlers/anthropic_compat.py
- ⚠️ agent/tools.py

---

## Architectural Notes

The implementation should be done in a way that allows for easy maintenance and updates, following the coding rules and best practices. The LiveKit integration should be done securely, and the Memory Kernel feature should be implemented in a way that ensures data consistency and integrity.

---

*Closes #664*

