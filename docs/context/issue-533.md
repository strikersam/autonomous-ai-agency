# Issue #533: SEO Agent improvement

_Generated: 2026-06-11_

## Context Plan — Issue #533: SEO Agent improvement

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement an SEO agent improvement based on the provided CSV file and Screaming Frog SEO Spider tool. The agent should be able to scan websites, identify issues, and provide recommendations for improvement. The issues to be identified include content-related issues, meta tags, headings, images, links, and security-related issues. The agent should prioritize the issues based on their severity and provide a report with recommendations for improvement. The report should include the issue name, issue type, issue priority, URLs affected, percentage of total issues, description, and how to fix the issue. The agent should also be able to fix some of the issues automatically, such as updating meta tags and headings. The implementation should be done in the `agent` module, specifically in the `loop.py` file, and should utilize the `Pydantic` models for API I/O and the `Langfuse` observability helper. The `router` module, specifically the `model_router.py` file, should also be updated to include the new SEO agent. The implementation should follow the coding rules, including type annotations on all public functions, no secrets in source, Pydantic models for all API I/O, async for all I/O, and logging with `logging` instead of `print`. The `docs/changelog.md` file should be updated to reflect the changes made in this implementation.

---

## TODO List

- [ ] **[M]** Update the `agent` module to include the new SEO agent `agent/loop.py`
- [ ] **[L]** Implement the SEO agent improvement based on the provided CSV file and Screaming Frog SEO Spider tool `agent/loop.py`
- [ ] **[M]** Update the `router` module to include the new SEO agent `router/model_router.py`
- [ ] **[S]** Update the `docs/changelog.md` file to reflect the changes made in this implementation `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` to test the implementation ``
- [ ] **[S]** Update the `README.md` file to include information about the new SEO agent `README.md`

---

## Relevant Files to Read First

- `agent/loop.py`
- `router/model_router.py`
- `docs/changelog.md`
- `README.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ router/model_router.py

---

## Architectural Notes

The implementation should be done in a way that allows for easy extension and modification of the SEO agent in the future. The `Langfuse` observability helper should be used to provide insights into the performance of the SEO agent. The `Pydantic` models should be used to define the structure of the API I/O.

---

*Closes #533*

