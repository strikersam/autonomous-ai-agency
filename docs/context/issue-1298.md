# Issue #1298: crispy-burn-in: CRISPY promotion readiness

_Generated: 2026-08-17_

## Context Plan — Issue #1298: crispy-burn-in: CRISPY promotion readiness

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://local-llm-server.onrender.com/api/autonomy/status` |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `z-ai/glm-5.2` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The GitHub issue is an auto-generated weekly burn-in status report for the CRISPY feature, produced by the `crispy-burn-in-check.yml` GitHub Actions workflow. It fetches data from the `/api/autonomy/status` endpoint's `crispy_run_history` field and evaluates five promotion criteria: total_runs (>=20), success_rate (>=0.8), window_days (>=7), no_phase_sequence_errors, and risky_module_review_signoff. The current state shows zero runs, zero success rate, null window days, and pending sign-off, reporting not ready for EXPERIMENTAL → stable promotion. The issue explicitly states no action is needed until all auto-checkable criteria are met.

---

## Decision

🛑 **REJECT** — nothing here belongs in this repository.

The issue body explicitly states 'No action needed until the gap reports All auto-checkable burn-in criteria met.' The current gap shows total_runs=0/20, success_rate=0.00%/80%, window_days=None/7 — the feature has not been exercised yet. Implementing promotion logic or status tracking changes now would be premature: the burn-in criteria are already defined and evaluated by the existing workflow and endpoint. The CRISPY feature needs to accumulate actual run history (20+ runs over 7+ days at 80%+ success rate) before any code change is warranted. There is no linked external artifact to evaluate — the issue is a status snapshot, not a change request. Building new code to 'help' CRISPY meet its burn-in criteria would violate rule 1 (do not change user-visible behaviour that was not requested) and would be inventing a feature to justify the issue.

---

## What was considered

This issue is an auto-generated CRISPY burn-in status report that explicitly states no action is needed. The issue body says: 'No action needed until the gap reports All auto-checkable burn-in criteria met.' The current status shows total_runs=0/20, success_rate=0.00%/80%, window_days=None/7 — the CRISPY feature has not accumulated any run history yet.

Considered and ruled out:
1. Implementing promotion logic — the existing `crispy-burn-in-check.yml` workflow and `/api/autonomy/status` endpoint already evaluate all five criteria (total_runs, success_rate, window_days, no_phase_sequence_errors, risky_module_review_signoff) and produce the gap report. No new evaluation logic is needed.
2. Seeding test data to meet thresholds — this would falsify burn-in results and violate the integrity of the promotion gate. The criteria exist to ensure real operational history before stable promotion.
3. Modifying the CRISPY agent loop to increase run frequency — the issue does not request this, and changing agent behaviour to meet burn-in thresholds would defeat the purpose of the gate.
4. Adding documentation about the burn-in process — the issue is a status snapshot, not a documentation request.

The correct action is to wait for the CRISPY feature to accumulate genuine run history. The weekly workflow will re-evaluate and update this issue automatically. When the gap reports 'All auto-checkable burn-in criteria met,' a separate issue will request the promotion with risky-module-review sign-off.

No code changes are warranted at this time.

---

## Architectural Notes

Assuming the `/api/autonomy/status` endpoint and `crispy_run_history` field already exist in the codebase (unverified — the issue references them as the data source, but I could not confirm their implementation paths from the provided context). Assuming `crispy-burn-in-check.yml` exists in `.github/workflows/` (unverified — referenced in the issue as the auto-generator). No TODOs modify any modules, so no risk flags are raised. The issue is a monitoring status snapshot with an explicit 'no action needed' directive.

---

## Quality Gate

⚠️ **1 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation

---

*Closes #1298*

