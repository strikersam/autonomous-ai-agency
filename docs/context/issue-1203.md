# Issue #1203: crispy-burn-in: CRISPY promotion readiness

_Generated: 2026-08-03_

## Context Plan — Issue #1203: crispy-burn-in: CRISPY promotion readiness

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://local-llm-server.onrender.com/api/autonomy/status` |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `z-ai/glm-5.2` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The linked artifact is an auto-generated GitHub issue body reporting CRISPY promotion readiness status for autonomous-ai-agency. It contains a JSON evaluation fetched from the autonomy status endpoint showing the CRISPY agent loop is not yet ready for EXPERIMENTAL to stable promotion. The burn-in criteria are failing because total_runs=0 (threshold >=20), success_rate=0.0% (threshold >=80%), and window_days=null (threshold >=7), while phase_sequence_errors=0 already passes. The issue explicitly states 'No action needed until the gap reports all auto-checkable burn-in criteria met,' indicating this is a monitoring-only issue awaiting natural run accumulation.

---

## Decision

🛑 **REJECT** — nothing here belongs in this repository.

The issue body explicitly states 'No action needed until the gap reports All auto-checkable burn-in criteria met.' This is an auto-generated status report from a weekly CI workflow, not a feature request or bug report. The gaps (total_runs=0, success_rate=0.0%, window_days=null) are data-driven metrics that will resolve naturally as CRISPY runs accumulate over time. There is no code change to make: the evaluation logic correctly reports the criteria as unmet, the thresholds are appropriate, and the issue itself instructs waiting for natural burn-in. Inventing a feature to force-populate run history would violate the Golden Rule (CLAUDE.md §0) by changing user-visible behavior without an explicit request, and would fabricate burn-in data rather than letting real telemetry accumulate.

---

## What was considered

This issue was reviewed and rejected. The issue is an auto-generated CRISPY burn-in status report from .github/workflows/crispy-burn-in-check.yml. It polls the /api/autonomy/status endpoint and reports whether the CRISPY agent loop meets promotion criteria (total_runs >= 20, success_rate >= 80%, window_days >= 7, no phase sequence errors, risky-module-review sign-off). The current evaluation shows total_runs=0, success_rate=0.0%, window_days=null — all data-driven gaps that resolve naturally as CRISPY runs accumulate. The issue body explicitly states: 'No action needed until the gap reports All auto-checkable burn-in criteria met.' No code change is warranted. The evaluation logic in the autonomy status endpoint is functioning correctly — it properly reports criteria as unmet when there is insufficient run history. Forcing run history to populate artificially would violate CLAUDE.md §0 (no user-visible behavior changes without explicit request) and would fabricate burn-in evidence rather than collecting genuine telemetry. The next weekly check will re-evaluate automatically. When CRISPY has accumulated 20+ runs over 7+ days with an 80%+ success rate, the issue will update to report readiness, at which point a separate promotion PR with risky-module-review sign-off becomes the appropriate action.

---

## Architectural Notes

Assuming the CRISPY agent loop is actively scheduled and will accumulate runs over time (unverified) — if the loop is not being triggered, the burn-in criteria will never be met and a separate investigation into loop scheduling would be needed. The issue references the endpoint at the autonomous-ai-agency backend and the crispy_run_history field, which maps to the existing autonomy status infrastructure in this codebase. The risky_module_review_signoff criterion (currently 'pending') requires a human sign-off on the promotion PR and cannot be auto-resolved.

---

## Quality Gate

⚠️ **1 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation

---

*Closes #1203*

