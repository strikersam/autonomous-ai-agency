# Issue #543: QA Agents Hardening

_Generated: 2026-06-12_

## Context Plan — Issue #543: QA Agents Hardening

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement a setup where multiple quality agents are deployed with various skills to perform different levels of testing (performance, functional, automation e2e via a browser, unit, api, security, accessibility, etc.) once a company URL is onboarded. Ensure that bugs are logged in a recurring manner and issues are raised and fixed. The platform should be continuously iterated and tested at various levels (smoke, regression, etc.), and the reliability of the system should keep improving over time while new features are added in parallel. To achieve this, modify the `agent/loop.py` file to include a planning phase that determines the required testing skills and deploys the corresponding agents. Update the `models.py` file to include Pydantic models for the testing results and bug reports. Create a new file `test_results.py` to handle the logging and tracking of test results. Modify the `router/model_router.py` file to route the testing requests to the appropriate agents. Use type annotations on all public functions, and ensure that all config is via environment variables. Use Pydantic models for all API I/O, and implement async I/O for all handlers and agent methods. Log with `logging` instead of `print`. Update the `docs/changelog.md` file under the [Unreleased] section to reflect the changes made. Run `pytest -x` before and after making changes to ensure that the tests pass.

---

## TODO List

- [ ] **[M]** Modify the `agent/loop.py` file to include a planning phase `agent/loop.py`
- [ ] **[S]** Update the `models.py` file to include Pydantic models for testing results and bug reports `models.py`
- [ ] **[M]** Create a new file `test_results.py` to handle logging and tracking of test results `test_results.py`
- [ ] **[M]** Modify the `router/model_router.py` file to route testing requests to appropriate agents `router/model_router.py`
- [ ] **[S]** Update the `docs/changelog.md` file under the [Unreleased] section `docs/changelog.md`
- [ ] **[S]** Run `pytest -x` before and after making changes ``

---

## Relevant Files to Read First

- `agent/loop.py`
- `models.py`
- `test_results.py`
- `router/model_router.py`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ key_store.py

---

## Architectural Notes

Assuming that the company URL onboarding process is already implemented, and the testing skills and agents are already defined. The implementation should focus on deploying the agents, logging bugs, and raising issues. The reliability of the system should be improved by continuously testing and iterating on the platform.

---

*Closes #543*

