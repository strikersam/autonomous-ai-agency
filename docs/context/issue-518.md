# Issue #518: Auto-ingest repo knowledge on connect: README, changelog, docs/knowledge into wiki

_Generated: 2026-06-11_

## Context Plan — Issue #518: Auto-ingest repo knowledge on connect: README, changelog, docs/knowledge into wiki

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

Implement the auto-ingestion of repository knowledge into the platform's wiki. This involves creating a new endpoint to handle the ingestion of README.md, docs/changelog.md, docs/knowledge/*.md, and docs/runbooks/*.md files from a connected GitHub repository. The ingestion process should occur when a repository is connected, on every merge to the default branch, and when a manual 'Sync now' button is clicked on the Knowledge screen. Use a deterministic slug mapping based on the file path to update existing wiki pages instead of creating duplicates. Tag ingested pages with 'repo-linked' and the source path, and display the last-synced commit SHA on the page. Modify the `proxy.py` file to add a new route for the ingestion endpoint, and update the `models.py` file to include Pydantic models for the ingestion request and response. Create a new file `ingest_repo_knowledge.py` to handle the ingestion logic, and update the `agent/loop.py` file to trigger the ingestion process on repository connection and default branch merges. Use the `logging` module for logging and follow the coding rules outlined in the project guide.

---

## TODO List

- [ ] **[M]** Create a new endpoint for ingesting repository knowledge `proxy.py`
- [ ] **[S]** Define Pydantic models for ingestion request and response `models.py`
- [ ] **[L]** Implement ingestion logic in a new file `ingest_repo_knowledge.py`
- [ ] **[M]** Update agent/loop.py to trigger ingestion on repository connection and default branch merges `agent/loop.py`
- [ ] **[S]** Add a manual 'Sync now' button to the Knowledge screen `remote-admin/index.html`
- [ ] **[S]** Update docs/changelog.md to reflect changes `docs/changelog.md`
- [ ] **[S]** Run pytest -x before and after changes ``

---

## Relevant Files to Read First

- `proxy.py`
- `models.py`
- `ingest_repo_knowledge.py`
- `agent/loop.py`
- `remote-admin/index.html`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ proxy.py

---

## Architectural Notes

Assuming the GitHub repository connection and default branch merge events are already handled by the agent/loop.py file. The ingestion logic should handle errors and exceptions properly, and the Pydantic models should be defined according to the project's coding rules.

---

*Closes #518*

