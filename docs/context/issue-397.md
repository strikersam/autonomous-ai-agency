        # Issue #397: 🤖 Autonomous Agency Setup — Changes Ready for Review

        _Generated: 2026-06-05_

                ## Context Plan — Issue #397: 🤖 Autonomous Agency Setup — Changes Ready for Review

        > Auto-generated by the **issue-context-generator** workflow.
        > This is a **DRAFT PR** — implement by triggering the Process Quick Note
        > workflow or by opening a Claude Code session on this branch.

        ---

        ## Implementation Prompt

        You are implementing changes for the autonomous agency setup as described in GitHub issue #397. Your task is to:

1. Fix `direct_chat.py` by moving the module docstring before `from __future__ import annotations` to comply with Python import order rules.
2. Clean up `.claude/state/improvement-state.json` by removing Git merge conflict markers and keeping only the HEAD version.
3. Re-enable the following workflows that were previously QUARANTINED:
   - `agency-cycle.yml`: runs every 6 hours, performing CEO assessment and agent-based auto-fixes
   - `continuous-improvement.yml`: daily at 09:00 UTC, scans for FIXME/TODO items and auto-manages GitHub issues
   - `weekly-trend-digest.yml`: Monday 08:00 UTC, fetches tech trends and creates digest issues
   - `ci-failure-autofix.yml`: triggers on CI failures, generates patches via Claude Sonnet 4.6
4. Add the new workflow `.github/workflows/perplexity-maintenance.yml` which includes:
   - Test suite health checks
   - Bandit security scans
   - Stale branch/dependabot PR tracking
   - Auto-merge of safe dependabot patches
   - Weekly health summaries posted to tracking issues
5. Create `AUTONOMOUS_AGENCY_SETUP.md` with complete operator documentation.
6. Ensure all required GitHub secrets are properly documented and used.

Follow these coding rules:
- Use `from __future__ import annotations` for all public functions
- All configuration must come from environment variables; no hardcoded secrets
- Use Pydantic models for API I/O
- All I/O operations must be async
- Log using the `logging` module, not `print`
- Update `docs/changelog.md` under [Unreleased]
- Run `pytest -x` before and after changes

        ---

        ## TODO List

        - [ ] **[S]** Fix direct_chat.py import order by moving module docstring before `from __future__ import annotations` `direct_chat.py`
- [ ] **[S]** Clean up .claude/state/improvement-state.json by removing merge conflict markers `.claude/state/improvement-state.json`
- [ ] **[M]** Re-enable agency-cycle.yml workflow with 6-hour schedule `.github/workflows/agency-cycle.yml`
- [ ] **[M]** Re-enable continuous-improvement.yml workflow with daily 09:00 UTC schedule `.github/workflows/continuous-improvement.yml`
- [ ] **[M]** Re-enable weekly-trend-digest.yml workflow with Monday 08:00 UTC schedule `.github/workflows/weekly-trend-digest.yml`
- [ ] **[M]** Re-enable ci-failure-autofix.yml workflow for CI failure triggered fixes `.github/workflows/ci-failure-autofix.yml`
- [ ] **[L]** Add new perplexity-maintenance.yml workflow with Tuesday 07:00 UTC schedule `.github/workflows/perplexity-maintenance.yml`
- [ ] **[M]** Create AUTONOMOUS_AGENCY_SETUP.md with complete operator documentation `AUTONOMOUS_AGENCY_SETUP.md`
- [ ] **[S]** Update docs/changelog.md with new feature entries `docs/changelog.md`
- [ ] **[S]** Verify all changes with pytest -x before committing ``

        ---

        ## Relevant Files to Read First

        - `direct_chat.py`
- `.claude/state/improvement-state.json`
- `.github/workflows/agency-cycle.yml`
- `.github/workflows/continuous-improvement.yml`
- `.github/workflows/weekly-trend-digest.yml`
- `.github/workflows/ci-failure-autofix.yml`
- `.github/workflows/perplexity-maintenance.yml`
- `AUTONOMOUS_AGENCY_SETUP.md`

        ---

        ## Risk Flags

        - ⚠️ admin_auth.py
- ⚠️ key_store.py
- ⚠️ agent/tools.py

        ---

        ## Architectural Notes

        Implementation should focus on re-enabling existing workflows and adding new ones as described. Pay special attention to auth module security and ensure no hardcoded secrets are introduced. All configuration must come from environment variables.

        ---

        *Closes #397*

