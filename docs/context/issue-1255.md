# Issue #1255: crispy-burn-in: CRISPY promotion readiness

_Generated: 2026-08-10_

## Context Plan — Issue #1255: crispy-burn-in: CRISPY promotion readiness

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://local-llm-server.onrender.com/api/autonomy/status` |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `z-ai/glm-5.2` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The linked artifact is an auto-generated GitHub issue from the crispy-burn-in-check workflow. It reports that the CRISPY agent loop is not yet ready for EXPERIMENTAL to stable promotion because its burn-in criteria are unmet: total_runs=0 (needs >=20), success_rate=0.0% (needs >=80%), window_days=None (needs >=7). The issue explicitly states 'No action needed until the gap reports all auto-checkable burn-in criteria met' and that the next weekly check will update it. The full evaluation JSON shows phase_sequence_errors=0 (met) and risky_module_review_signoff=pending (unmet, but requires human review on the promotion PR).

---

## Decision

🛑 **REJECT** — nothing here belongs in this repository.

The issue body explicitly states 'No action needed until the gap reports All auto-checkable burn-in criteria met.' The unmet criteria (total_runs, success_rate, window_days) are all at zero because no CRISPY runs have executed yet — they are runtime data gaps, not code defects. The only unmet criterion that could involve code is whether the run history tracking itself is broken, but the issue shows the evaluation JSON was successfully fetched and structured correctly, meaning the tracking infrastructure works. The remaining unmet criterion (risky_module_review_signoff) requires human sign-off on a promotion PR, which is a process gate, not a code change. Writing code to fabricate run history or force promotion would violate CLAUDE.md §0 (no user-visible behaviour changes unless explicitly requested) and the burn-in gate's purpose. The correct action is to wait for the weekly workflow to accumulate real runs.

---

## What was considered

This issue was considered for adoption and rejected. The reasoning chain:

1. The issue is auto-generated by .github/workflows/crispy-burn-in-check.yml and explicitly says 'No action needed until the gap reports All auto-checkable burn-in criteria met.' The current gap (total_runs=0, success_rate=0.0%, window_days=None) means no CRISPY agent runs have occurred yet — these are runtime data conditions, not code bugs.

2. The evaluation JSON was successfully fetched from the /api/autonomy/status endpoint and parsed correctly, confirming the burn-in tracking infrastructure (crispy_run_history) is functional. A broken tracker would produce malformed or missing JSON, not a well-structured zero-state report.

3. The unmet criteria break down as: total_runs (needs >=20, currently 0) — requires actual CRISPY loop executions over time; success_rate (needs >=80%, currently 0.0%) — derivative of total_runs; window_days (needs >=7, currently None) — requires runs spread across at least 7 calendar days; risky_module_review_signoff (pending) — a human process gate on the promotion PR, not automatable.

4. Fabricating run history or forcing promotion would defeat the burn-in gate's purpose (proving the loop is stable over real usage) and would violate CLAUDE.md §0.

5. The only scenario requiring code changes would be if the CRISPY loop itself is failing to execute when triggered — but that would be a separate bug issue, not this burn-in status report. The issue does not report execution failures, only zero executions.

No TODOs are warranted. The next weekly check will re-evaluate. If after several weeks the counts remain at zero, a separate investigation into why CRISPY runs are not being triggered would be appropriate — but that is not this issue.

---

## Architectural Notes

Assuming the CRISPY loop is functional and simply has not been triggered enough times (unverified) — if wrong, meaning the loop is broken and cannot execute, then a separate issue should be filed to debug the CRISPY execution path. The issue's own instruction ('No action needed until...') confirms this is a waiting state, not an action state.

---

## Quality Gate

⚠️ **1 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation

---

*Closes #1255*

