# Issue #398: Agency: Cannot Fix Tests — 2026-06-05

_Generated: 2026-06-05_

## Context Plan — Issue #398: Agency: Cannot Fix Tests — 2026-06-05

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

You are working on a self-hosted, OpenAI-compatible proxy for Ollama with FastAPI, async I/O, Bearer-token auth, model routing, Langfuse observability, multi-agent orchestration, Telegram bot, and a React/Next.js admin dashboard. The issue is that agency-related tests are failing. Your task is to identify and fix the root cause of the test failures in the agent module. Start by running `pytest -x` to reproduce the issue. Examine the failing tests in `tests/` to understand which agent functionalities are broken. Focus on `agent/loop.py` (AgentRunner logic), `agent/tools.py` (WorkspaceTools), and `agent/state.py` (agent session store). Review the agent's plan → execute → verify cycle implementation. Fix any logical errors, race conditions, or state management issues that are causing the tests to fail. Ensure all agent-related functions are async and properly handle I/O. Add proper error handling and logging where necessary. Update tests in `tests/` to reflect any fixed behavior. Run `pytest -x` after changes to confirm fixes. All changes must follow the project's coding rules: type annotations on all public functions, no secrets in source, Pydantic models for API I/O, async for all I/O, and logging instead of print statements. Reference `agent/CLAUDE.md` for agent-specific guidelines.

---

## TODO List

- [ ] **[S]** Run pytest to identify failing tests `tests/test_agent.py`
- [ ] **[M]** Identify root cause of agent test failures `agent/loop.py`
- [ ] **[L]** Fix AgentRunner logic in loop.py `agent/loop.py`
- [ ] **[M]** Stabilize WorkspaceTools in tools.py `agent/tools.py`
- [ ] **[M]** Verify agent session state management `agent/state.py`
- [ ] **[M]** Update failing tests to reflect fixes `tests/test_agent.py`
- [ ] **[S]** Run pytest to confirm all tests pass ``

---

## Relevant Files to Read First

- `agent/loop.py`
- `agent/tools.py`
- `agent/state.py`
- `tests/test_agent.py`
- `agent/CLAUDE.md`

---

## Risk Flags

- ⚠️ agent/loop.py
- ⚠️ agent/tools.py

---

## Architectural Notes

Focus on agent/loop.py and agent/tools.py as these are the most likely to have issues causing test failures. The agent's plan-execute-verify cycle is critical to stability.

---

*Closes #398*

