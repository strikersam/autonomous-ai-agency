        # Issue #379: quick-note:https://searchengineland.com/schema-markup-optimize-agentic-web-479080

        _Generated: 2026-06-05_

                ## Context Plan — Issue #379: quick-note:https://searchengineland.com/schema-markup-optimize-agentic-web-479080

        > Auto-generated by the **issue-context-generator** workflow.
        > This is a **DRAFT PR** — implement by triggering the Process Quick Note
        > workflow or by opening a Claude Code session on this branch.

        ---

        ## Implementation Prompt

        Act as a senior engineering architect. Analyze the URL https://searchengineland.com/schema-markup-optimize-agentic-web-479080 to understand how schema markup can enhance agentic web capabilities. Based on the article's insights about structured data and semantic web optimization, implement enhancements to improve model routing and agent intelligence. Specifically, enhance `agent/repowise.py` to incorporate schema-aware context packing by updating the `RepowiseIntelligence` class. The article suggests that structured data can optimize agent interactions - implement this by adding schema markup analysis to the context packing logic in `agent/repowise.py`. Update the `build_context` method in the `RepowiseIntelligence` class to analyze and include schema information from code files. Also enhance `agent/prompts.py` to include schema-aware prompt builders that can leverage structured data insights. Ensure all new functions have type annotations and follow the async I/O pattern. Reference the existing codebase patterns in `agent/repowise.py` and `agent/prompts.py` for consistency. Add appropriate logging using the `logging` module. Update the agent's context packing to consider schema markup in code files as part of its intelligence gathering. The enhancement should align with the existing multi-agent orchestration in `agent/loop.py` and maintain compatibility with the Langfuse observability in `langfuse_obs.py`. No secrets should be added to source; all config must be via environment variables as per the project's coding rules.

        ---

        ## TODO List

        - [ ] **[L]** Enhance RepowiseIntelligence to analyze schema markup in code files `agent/repowise.py`
- [ ] **[M]** Update context packing logic to include schema-aware intelligence `agent/repowise.py`
- [ ] **[L]** Modify prompt builders to leverage structured data insights `agent/prompts.py`
- [ ] **[M]** Update agent loop to utilize schema intelligence during planning/execution `agent/loop.py`
- [ ] **[M]** Add schema analysis capability to workspace tools for reading files `agent/tools.py`
- [ ] **[M]** Create tests for schema-aware context packing `tests/test_repowise.py`
- [ ] **[S]** Update Langfuse observability to trace schema analysis `langfuse_obs.py`
- [ ] **[S]** Update documentation with new schema markup features `docs/agent_schema_intelligence.md`
- [ ] **[S]** Add changelog entry for schema intelligence feature `docs/changelog.md`

        ---

        ## Relevant Files to Read First

        - `agent/repowise.py`
- `agent/prompts.py`
- `agent/loop.py`
- `agent/tools.py`
- `agent/models.py`

        ---

        ## Risk Flags

        - ⚠️ agent/loop.py
- ⚠️ agent/tools.py
- ⚠️ key_store.py

        ---

        ## Architectural Notes

        The implementation should focus on enhancing the agent's ability to understand and utilize structured data (schema markup) in code files to improve context packing and decision making. This aligns with the article's theme of using schema markup to optimize agentic web interactions.

        ---

        *Closes #379*

