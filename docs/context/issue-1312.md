# Issue #1312: Agency: Cannot Fix Tests — 2026-08-20

_Generated: 2026-08-21_

## Context Plan — Issue #1312: Agency: Cannot Fix Tests — 2026-08-20

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://docs.pytest.org/en/stable/how-to/capture-warnings.html |
| Fetch status | ✅ fetched — 6,000 chars of content |
| Generated by | NVIDIA NIM · `meta/llama-3.3-70b-instruct` |
| Repair rounds | 0 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The linked article describes how to capture warnings in pytest, including how to control which warnings are ignored, displayed, or turned into errors. It also explains how to use the @pytest.mark.filterwarnings mark to add warning filters to specific test items. The article provides examples of how to treat warnings as errors and how to ignore specific warnings. Additionally, it discusses the warning filter documentation and how to use the -W flag to control warnings.

---

## Decision

✅ **ADOPT** — build largely as described in the source.

The article provides valuable information on how to handle warnings in pytest, which can be useful for improving the test suite of the autonomous-ai-agency project.

---

## Implementation Prompt

The coding agent should implement the necessary changes to improve the handling of warnings in the test suite. This includes using the @pytest.mark.filterwarnings mark to add warning filters to specific test items and controlling which warnings are ignored, displayed, or turned into errors. The agent should also consider using the -W flag to control warnings and consult the warning filter documentation for more advanced use-cases.

---

## TODO List

- [ ] **[M]** Add @pytest.mark.filterwarnings to relevant test items — `tests/test_auth_me_regression.py`
      <br/>_Done when:_ The test suite no longer produces unnecessary warnings and the warnings are properly filtered
- [ ] **[S]** Use the -W flag to control warnings — `pytest.ini`
      <br/>_Done when:_ The test suite produces the expected warnings and errors

---

## Relevant Files to Read First

- `tests/test_auth_me_regression.py`
- `pytest.ini`

---

## Risk Flags

- ⚠️ `tests/test_auth_me_regression.py`

---

## Architectural Notes

The coding agent should consider the trade-offs between ignoring warnings and treating them as errors, and decide on the best approach based on the project's requirements.

---

## Quality Gate

✅ Passed every machine-checked rule in `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`.

---

*Closes #1312*

