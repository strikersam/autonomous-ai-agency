# Issue #493: Use the https://github.com/mvanhorn/last30days-skill skill to get the trend updated

_Generated: 2026-06-09_

## Context Plan — Issue #493: Use the https://github.com/mvanhorn/last30days-skill skill to get the trend updated

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Implementation Prompt

You are to implement integration with the last30days-skill to analyze current AI/ML trends and update the repository accordingly. First, create a new module `trend_analysis.py` to interface with the last30days-skill. This should include functions to install the skill using the provided CLI, execute trend searches, and parse results. Use subprocess to run the skill's CLI commands. The trend analysis should focus on AI/ML topics relevant to this project's stack (FastAPI, Ollama, Langfuse, etc.). Create a new endpoint `/trends` in `proxy.py` that triggers the trend analysis. The endpoint should be protected and only accessible to admin users. Results should be logged via `langfuse_obs.py`. Additionally, update the admin dashboard (`remote-admin/`) to display a summary of recent trends. Follow all project coding rules: use type annotations, async for I/O, and proper error handling. Store trend results in a new `trends/` directory with files like `trend_summary.md`. The skill should be executed in an async background task. Reference the codebase map in CLAUDE.md to understand where to place new files and how the system works.

---

## TODO List

- [ ] **[L]** Create trend_analysis.py module to interface with last30days-skill CLI `trend_analysis.py`
- [ ] **[M]** Implement function to execute trend search for 'local LLM development' and related topics `trend_analysis.py`
- [ ] **[M]** Add trend analysis results parsing and storage in trends/ directory `trend_analysis.py`
- [ ] **[M]** Create new endpoint in proxy.py to trigger trend analysis `proxy.py`
- [ ] **[L]** Add trend summary display to admin dashboard in remote-admin/ `remote-admin/src/app/page.tsx`
- [ ] **[M]** Update documentation in docs/ and CLAUDE.md about the new trend analysis feature `docs/architecture/ADR-005-trend-analysis.md`
- [ ] **[M]** Add tests for the new endpoint and trend analysis functions `tests/test_trend_analysis.py`
- [ ] **[S]** Update changelog and version `docs/changelog.md`

---

## Relevant Files to Read First

- `proxy.py`
- `trend_analysis.py`
- `remote-admin/src/app/page.tsx`
- `docs/architecture/ADR-005-trend-analysis.md`

---

## Risk Flags

- ⚠️ proxy.py
- ⚠️ admin_auth.py

---

## Architectural Notes

The last30days-skill integration should be implemented as a new module that can be executed as a background task. The results should be stored and made accessible through the admin dashboard. Consider rate limiting and caching for the trend analysis endpoint. The skill requires BYOK (bring your own keys) for various platforms, so the setup should guide the user to configure these if they want full functionality. For initial implementation, focus on the GitHub and HN sources which work with zero config.

---

*Closes #493*

