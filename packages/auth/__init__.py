"""packages.auth — unified authentication platform.

Consolidates JWT, OAuth, API keys, service tokens, and RBAC into one package.
Every auth decision flows through here — no duplicate auth logic anywhere.
"""
