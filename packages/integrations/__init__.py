"""packages/integrations — clients for third-party platforms the agency runs on.

Each module here wraps exactly one external platform and exposes a typed,
testable facade. Nothing in this package reads environment variables directly;
configuration comes from ``packages.config.settings``.
"""
